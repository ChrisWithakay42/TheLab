apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: snapshot-hourly
  namespace: longhorn-system
spec:
  name: snapshot-hourly
  cron: "0 * * * *"
  task: "snapshot"
  retain: 48
  groups:
  - default
  labels:
    longhorn-backup: "true"


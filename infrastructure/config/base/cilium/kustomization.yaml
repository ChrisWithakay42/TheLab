apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - ip-pool.yaml
  - l2policy.yaml
  - bgp-peering-conf.yaml
configMapGenerator:
- name: cilium-values
  files:
  - values.yaml
generatorOptions:
  disableNameSuffixHash: true

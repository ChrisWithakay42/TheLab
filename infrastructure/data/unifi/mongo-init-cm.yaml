apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-init-script
  namespace: unifi-db
data:
  init-mongo.js: |
    db = db.getSiblingDB('unifi');
    db.createUser({
      user: 'unifi',
      pwd: 'unifipassword',
      roles: [{ role: 'readWrite', db: 'unifi' }],
    });

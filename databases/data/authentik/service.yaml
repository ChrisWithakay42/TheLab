apiVersion: v1
kind: Service
metadata:
  name: pgbouncer-service
  namespace: authentik
spec:
  type: ClusterIP
  ports:
    - port: 6432
      targetPort: 6432
      name: pgbouncer
  selector:
    app.kubernetes.io/name: pgbouncer

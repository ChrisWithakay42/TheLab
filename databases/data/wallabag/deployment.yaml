apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: wallabag-db
  labels:
    app: postgres
spec:
  replicas: 1
  strategy:
    type: Recreate 
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          # --- OPTIMIZATION --- #
          args:
            - "-c"
            - "shared_buffers=64MB"
            - "-c"
            - "max_connections=50"
            - "-c"
            - "work_mem=2MB"
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"  
              cpu: "500m"      
          envFrom:
            - secretRef:
                name: wallabag-postgres-secret
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          startupProbe:
            exec:
              command:
                - pg_isready
                - -U
                - "wallabag"
            failureThreshold: 30
            periodSeconds: 5
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - "wallabag"
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: wallabag-postgres-pvc

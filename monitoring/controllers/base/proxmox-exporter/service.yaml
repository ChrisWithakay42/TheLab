apiVersion: v1
kind: Service
metadata:
  name: proxmox-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: proxmox-exporter
spec:
  ports:
    - name: http
      port: 9221
      targetPort: http
  selector:
    app.kubernetes.io/name: proxmox-exporter
